/**
 * Shown on the Account screen so anyone testing can confirm at a glance which
 * build a phone is actually running.
 *
 * This tracks the JavaScript bundle, which is where nearly every change lands —
 * if this reads what you expect, the new code is installed. Keep it in step
 * with versionName in android/app/build.gradle.
 */
export const APP_VERSION = '2.0';
